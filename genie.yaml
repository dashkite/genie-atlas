coffee:
  targets:
    - node

drn:

  # we only need this because this module
  # doesn't use genie-sky
  resolvers:
    - "@dashkite/drn-sky"

  yaml:
    targets:
      node:
        - preset: js
          glob: "src/**/*.yaml"
